# apps/client (Frozen Reference)

This frontend is frozen and kept only for reference during migration.

## Status

- No new feature development should happen in this app.
- Use `apps/frontend` for all active V1 work.
- This codebase can be used to inspect old behavior while rebuilding flows in the new frontend.
